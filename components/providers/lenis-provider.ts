"use client";

export * from "lenis/react";

// global provider(optional) for the lenis scrolling library
// use usage in layout.tsx
//import { ReactLenis } from "@/providers/lenis-provider";
// wrap all content with <ReactLenis root>{content} <ReactLenis />
